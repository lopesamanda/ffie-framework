NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Shared password for /admin moderation (httpOnly cookie session).
# Required to sign in and publish/reject submissions. Server-only — do not prefix with NEXT_PUBLIC_.
ADMIN_PASSWORD=
